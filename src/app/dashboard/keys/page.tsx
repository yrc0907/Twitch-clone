import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Gamehub | Keys",
  description: "Manage your API keys",
};

export default function KeysPage() {
  return (
    <div className="h-full">
      {/* Content for keys page */}
    </div>
  );
} 
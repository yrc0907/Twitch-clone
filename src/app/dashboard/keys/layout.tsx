import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Gamehub | Keys & URLs",
  description: "Manage your API keys",
};

export default function KeysLayout({ children }: { children: React.ReactNode }) {
  return children;
} 
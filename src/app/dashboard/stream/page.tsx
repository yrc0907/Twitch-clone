import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Gamehub | Stream",
  description: "Manage your streams",
};

export default function StreamPage() {
  return (
    <div className="h-full">
      {/* Content for stream page */}
    </div>
  );
} 
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Gamehub | Creator Dashboard",
  description: "Gamehub creator dashboard",
};

export default function DashboardPage() {
  return (
    <div className="h-full">
      {/* This is the empty content area shown in the design */}
      {/* We're not adding content here as the screenshot shows an empty content area */}
    </div>
  );
} 
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Gamehub | Chat",
  description: "Chat with your community",
};

export default function ChatPage() {
  return (
    <div className="h-full">
      {/* Content for chat page */}
    </div>
  );
} 